<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Echo" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Warm cream launch background (instead of a white flash) -->
        <item name="android:windowBackground">@color/echo_cream</item>
    </style>
</resources>